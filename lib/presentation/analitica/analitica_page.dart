import 'package:flutter/material.dart';

class AnaliticaPage extends StatelessWidget {
  const AnaliticaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Analitica'),
      ),
    );
  }
}
