import 'package:flutter/material.dart';

class ProteccionPage extends StatelessWidget {
  const ProteccionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Proteccion'),
      ),
    );
  }
}
